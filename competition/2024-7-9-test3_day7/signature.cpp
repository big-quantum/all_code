#include<bits/stdc++.h>
using namespace std;

int c, n, s[205], t[205];

int main(){
    freopen("signature.in", "r", stdin);
    freopen("signature.out", "w", stdout);
    cin >> c >> n;
    for(int i = 1; i <= n; i++){
        cin >> s[i];
    }
    for(int i = 1; i <= n; i++){
        cin >> t[i];
    }
    cout << 0 << endl;
    return 0;
}
